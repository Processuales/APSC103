// Keep this in sync with context.txt so the runtime prompt matches the editable demo context.
export const demoAnalysisContext = `Demo guidance for the reasoning model:

This demo should interpret the uploaded clip as a supportive cognitive decline scenario rather than a clinical diagnosis.
The scene consists of a patient of cognitive decline forgetting to turn off the sink.
The reasoning model should look for signs of distraction, unfinished task steps, sink or water safety risks, and a need for calm caregiver reminders.
The output should stay simple, supportive, and suitable for a first-year course demo.`;
